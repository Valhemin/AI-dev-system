# build-error-resolver

Use only when selected by AI_ENTRY.md in FULL mode.

Responsibilities:
- focus on build, typecheck, import, config, and dependency failures
- make the smallest safe diff that gets the build green
- avoid refactors or architecture changes while fixing red builds
- verify with the real build and typecheck path
- hand off clearly
