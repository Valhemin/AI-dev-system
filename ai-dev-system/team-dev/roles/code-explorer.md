# code-explorer

Use only when selected by AI_ENTRY.md in FULL mode.

Responsibilities:
- deeply analyze existing features before new implementation starts
- trace entry points, execution paths, architecture layers, and dependencies
- produce compact exploration notes with key files and recommendations
- avoid speculative redesigns during discovery
- hand off clearly
