# doc-updater

Use only when selected by AI_ENTRY.md in FULL mode.

Responsibilities:
- update docs, codemaps, and guides from source-of-truth files
- keep README, runbook, and developer docs aligned with code changes
- verify examples, commands, and links where practical
- avoid speculative documentation that code does not support
- hand off clearly
